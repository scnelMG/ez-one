name: Encoding

on:
  pull_request:
  push:

jobs:
  check-encoding:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check UTF-8 encoding
        shell: pwsh
        run: .\tools\check-encoding.ps1
